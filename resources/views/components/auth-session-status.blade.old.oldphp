@props([
    'status',
    'icon' => 'cil-warning',
    'type' => 'alert-primary'
])

@if ($status)
    <div {{ $attributes->merge(['class' => "d-flex flex-row align-items-center $type"])}} role="alert">
        <span {{ $attributes->merge(['class' => "me-2 $icon"])}}></span>
        <span class="fs-7">
            {{ $status }}
        </span>
    </div>
@endif
