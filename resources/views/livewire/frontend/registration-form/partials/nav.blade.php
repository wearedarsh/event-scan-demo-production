<header class="bg-[var(--color-surface)] shadow-sm fixed w-full z-50">
  <div class="max-w-6xl mx-auto px-6 py-4 flex items-center justify-between">
    <a href="/" class="flex items-center gap-3">
      <img src="{{ client_setting('branding.frontend.registration.logo.path') }}" alt="{{ client_setting('general.customer_friendly_name') }} logo" class="{{ client_setting('branding.frontend.registration.logo.custom_classes') }}" />
    </a>
  </div>
</header>